{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run db:generate"
  },
  "deploy": {
    "startCommand": "npx tsx src/lib/jobs/worker.ts",
    "restartPolicyType": "on_failure",
    "restartPolicyMaxRetries": 10
  }
}
